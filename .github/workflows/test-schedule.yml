name: Schedule Test

on:
  schedule:
    - cron: "*/10 * * * *"  # 每10分钟跑一次
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Echo time
        run: echo "Scheduled run at $(date -u)"
